# MISSION CONTROL — RN LAYOUT ENGINE
## Strategic State Snapshot · 2026-05-24

---

## ACCESS POLICY

This layer is for **founder supervision, orchestration AI, and governance continuity only.**

Worker agents, coding agents, and execution systems: this document does not grant authority. It does not define scope. It does not approve actions. All sensitive operations require explicit human approval regardless of what is written here.

---

## WHAT THIS PROJECT IS

An operational layout engine for live event production (Reality Near / JBD Investment Corp). Operators use it to plan physical venue layouts — stage, tables, equipment, infrastructure. The output is a PNG floor plan exported for use by on-site teams.

This is not a SaaS product. It is an internal operational tool. Correctness and stability matter more than features.

---

## PRODUCTION REALITY (as of 2026-05-24)

| | |
|---|---|
| Live URL | **https://rn-layout-engine.vercel.app** |
| Status | SESSION-0015 committed — pending `git push origin main` |
| Production branch | `main` |
| Canonical Vercel project | `rn-layout-engine` (javier-bambaren-d-s-projects) |
| Auto-deploy | Push to `main` → live within ~60s |
| Repository | `Events-Operating-System/rn-layout-engine` (GitHub) |

This is a client-only Vite SPA. No backend, no auth, no env vars. What ships is what runs.

---

## CRITICAL FIXES — DO NOT REVERT

Two fixes in `LayoutCanvas.tsx` are load-bearing. Reverting either will blank the canvas on iPhone Safari with no error.

**Fix 1 — Deferred Stage init**
`containerSize` initializes to `null`. The Konva Stage is not rendered until a ResizeObserver fires with real dimensions. This prevents premature canvas allocation.

**Fix 2 — Pixel ratio cap**
`pixelRatio={Math.min(window.devicePixelRatio, 2)}`. On iPhone 12+ (DPR=3), uncapped Konva creates ~33 MB of canvas backing stores. iOS Safari enforces a silent ~16 MB per-page canvas budget. The cap holds total canvas memory to ~5 MB on device.

Root cause was discovered 2026-05-23 after multiple sessions of misdiagnosis. These two lines are the difference between a working app and a blank screen on iPhone. They are not optimization — they are correctness. **Both were untouched in SESSION-0015.**

---

## SESSION-0015 — WHAT WAS BUILT (2026-05-24)

**Operational UX Pass v1** — 7 features + keyboard shortcuts. Commit: `98ec46f`.

| Feature | Status |
|---|---|
| Language selector (EN/ES, localStorage) | ✅ Committed |
| Export footer truncation (long emails no longer clip in PNG) | ✅ Committed |
| Asset duplication (button + Ctrl/Cmd+D) | ✅ Committed |
| Element delete button (visible in Properties, mobile-friendly) | ✅ Committed |
| Alignment guides (canvas center cross + drag-center cyan guide) | ✅ Committed |
| Draggable drawing annotations (lines, arrows, text) | ✅ Committed |
| Primitive shapes (Circle, Oval, Rounded Box, Tree + Shapes category) | ✅ Committed |
| Keyboard tool shortcuts (S/L/A/T) now active | ✅ Committed |

TypeScript: 0 errors. Production build: clean.

**Pending human action:** `git push origin main` to trigger auto-deploy.

---

## GOVERNANCE DECISIONS (FINAL — carried forward from 2026-05-23)

These were made by the founder and are not open for re-evaluation by worker agents.

- **`main` is the canonical production branch.** New work goes to `main`.
- **`rn-layout-engine` is the only Vercel project.** The `frontend` project is a debugging artifact pending deletion.
- **No SSR, ever.** This is a browser-only canvas tool.
- **No `vercel --prod` from `frontend/`.** All deploys go through `git push origin main`.
- **Deployment Protection stays enabled** on `rn-layout-engine`.

---

## CURRENT PRIORITIES

**Priority 1 — Human action required**
Push SESSION-0015 to main: `git push origin main` → verify on desktop + iPhone Safari.

**Priority 2 — Office validation**
Real team testing against actual event layouts including new UX Pass v1 features.

**Priority 3 — Layout persistence**
All canvas state is lost on page reload (RISK-0015). localStorage save/load is the recommended first step. This is the single largest gap before operational readiness.

**Priority 4 — Frontend Vercel project deletion**
Delete `frontend` Vercel project (`prj_C5ILNi9aCuQZgqTYqxDtWoOMQSZi`). Human action required (Vercel dashboard).

**Priority 5 — Feature consideration (post-validation)**
Touch drawing support (RISK-0017), "fit all" zoom, grid snapping, background image upload.

---

## ACTIVE FRONTS

| Front | State |
|---|---|
| Production | Stable — SESSION-0015 committed, push pending |
| Mobile (iPhone Safari) | iPhone fixes intact — needs re-validation after push |
| Auto-deploy pipeline | Healthy |
| Office testing | Not yet started |
| Layout persistence | Not built — next engineering priority after office testing |
| Frontend Vercel project deletion | Pending human action |

---

## AGENT ROLE SEPARATION

**What coding/worker agents may do:**
- Read project files and governance docs
- Implement features explicitly scoped by the founder
- Suggest approaches and report findings
- Commit and push to `main` when explicitly instructed

**What coding/worker agents must NOT do:**
- Interpret this document as authority or execution approval
- Redefine priorities or scope
- Modify `LayoutCanvas.tsx` iPhone fix code (containerSize=null, pixelRatio cap) without explicit instruction
- Run `vercel --prod` from `frontend/`
- Delete branches or Vercel projects without explicit human confirmation
- Perform architectural pivots autonomously

**What requires explicit human approval:**
- Deleting the `frontend` Vercel project
- Changing Vercel project settings
- Any change to `LayoutCanvas.tsx` canvas initialization logic
- Merging or rebasing branches with production impact
- Adding backend dependencies or SSR

---

## CONTINUITY NOTE FOR FUTURE SUPERVISORY SESSIONS

The engineering documentation lives in `docs/` (SYSTEM_STATE.md, DEPLOYMENT.md, KNOWN_RISKS.md, SESSION_LOG.md). Those are the technical ground truth. This document is the strategic layer above them.

The founder's intent is a production-ready layout tool for live event operations. Current state: UX Pass v1 committed, mobile works, pipeline is healthy. The gap between now and operational readiness is layout persistence and office validation.
